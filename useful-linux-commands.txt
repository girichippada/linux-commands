--checking file mount disk usage:
df -h

--checking free memory of the machine
free -h

--checking list of processes of the system
ps -aux


du -ah /dev/nvme0n1p1 | sort -rh | head -n 10

--find all files whose size is more than 100 MB
sudo find / -type f -size +50M -exec ls -lh {} \; | awk '{ print $9 ": " $5 }'

sudo truncate -s 0 /var/log/messages

--system logging service
sudo systemctl status rsyslog
sudo systemctl restart rsyslog

--journal logs:
ll /run/log/journal

--find files modified older than last 3 days
find /run/log/journal -type f -mtime +3

--delete
sudo find /run/log/journal -type f -mtime +3 -exec rm -f {} \;




list all files based on size of a folder
du -h /home/centos/logstash-8.9.0


--deleted index pattern in prod
edp-logstash-service.mds.repair_order.raw-*

replaced with:
dyw-edp-logstash-edp.service.mds.repair_order.raw-*-raw
created:
dyw-edp-logstash-edp.consumer.mds.customer.raw-*-raw
dyw-edp-logstash-edp.sales.mds.vehicle_sales.raw-*-raw

aws s3 cp s3://prod-confluent-edp-aws/dyw-logstash-pipelines/configs/pipelines/dyw-snapshot/dyw-snapshot.config . --region us-west-2
 
